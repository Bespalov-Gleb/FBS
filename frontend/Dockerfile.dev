FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .

# Vite слушает на 0.0.0.0 для доступа снаружи контейнера
ENV VITE_API_URL=http://localhost:8000/api/v1
EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
